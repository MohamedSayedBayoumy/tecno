import 'package:flutter/material.dart';

class CustomEmptyView extends StatelessWidget {
  const CustomEmptyView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}