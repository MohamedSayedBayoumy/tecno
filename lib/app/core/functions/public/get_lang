import 'package:get_storage/get_storage.dart';

String language() {
  final storage = GetStorage();
  final lang = storage.read('lang');
  if (lang == null) {

    storage.write('lang', 'ar');
    return 'ar';
  } else {

    return lang;
  }
}
